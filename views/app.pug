doctype html
html
  head
    meta(charset="utf-8")
    meta(name="generator" content="ArcadeStudioWebApp")
    meta(name="viewport" content="width=device-width, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0")
    title ArcadeStudio

    mixin mymeta(property, content)
      meta(property=property content=content)

    +mymeta('og:title', "ArcadeStudio")
    +mymeta('og:description', title)
    +mymeta('og:type', "website")
    +mymeta('og:image', image)
    +mymeta('og:url', slug)
    +mymeta('og:site_name', "arcade.studio")
    +mymeta('og:summary', title)
    +mymeta('twitter:site', "@_ArcadeStudio_")
    +mymeta('twitter:domain', "https://arcade.studio/")
    +mymeta('twitter:card', "summary")
    +mymeta('twitter:title', "ArcadeStudio")
    +mymeta('twitter:description', title)
    +mymeta('twitter:image', image)



    link(rel='stylesheet' href='/js/editor/libs/loading-bar/loading-bar.min.css')
    link(rel='stylesheet' href='/css/app.css')
  body
    .drag-ghost
    script.
      var app=!{JSON.stringify(app)};
      window.zipFileUrl=!{JSON.stringify(zipFileUrl)};
      window.publishingVersion=!{JSON.stringify(publishingVersion)};
    //- script(src='/js/lib/jquery.min.js')
    //- script(src='/js/lib/lottie_canvas.js')
    //- script(src='/js/lib/lottie-player.js')
    //- script(src="/js/app/js/vendor.min.js")
    //- script(src="/js/webapp.min.js")
    script(src="/js/webapp.versioned.min.js")
